/*
 * Minimal custom JavaScript for Assaf Cars
 *
 * This script is intentionally lightweight to keep page load times fast. It
 * provides a place to add custom interactions in the future. Currently it
 * simply logs a message once the DOM has fully loaded.
 */

document.addEventListener('DOMContentLoaded', function () {
    console.log('Assaf Cars UI loaded');
});